name: 'SBOM Generation and Scanning'
description: 'Generates a software bill of materials and scans for known vulnerabilities'
author: 'peter'
outputs:
  sbom-file:
    description: 'the syft generated sbom'
    value: ${{ steps.sbom-gen-and-scan.outputs.sbom-file }}
runs:
  using: 'docker'
  image: 'Dockerfile'
